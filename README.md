# DELAPORTE Alexy - 4IW2

### Lancement du projet

npm run start

npx @tailwindcss/cli -i ./public/css/input.css -o ./public/css/output.css --watch

### Limites de l'utilisation de l'IA pour l'implémentation de l'OAuth

Il a été très compliqué de trouver comment configurer correctement le projet Meta Developers.

L'IA indiquait des chemins inexistants, probablement présents sur une ancienne version de la page de Meta.
